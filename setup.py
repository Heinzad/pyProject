from setuptools import setup

setup(
    name='pyProject',
    version='0.1',
    description='A Skeleton structure for python projects.',
    author='Adam Heinz',
    #author_email='name@example.com',
    #packages=['my_package'],
    install_requires=[
        'python-dotenv',
    ],
)